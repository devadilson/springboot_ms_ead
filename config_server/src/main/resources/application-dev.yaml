server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devadilson/ead-config-server-repo #${}}
          default-label: main #${GIT_BRANCH}
          username: devadilson
          password: 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

ead:
  configServer:
    username: configserver
    password: 'ead123cs'
  serviceRegistry:
    username: serviceregistry
    password: 'ead123sr'

eureka:
  client:
    serviceUrl:
      defaultZone: 'http://${ead.serviceRegistry.username}:${ead.serviceRegistry.password}@localhost:8761/eureka'
  instance:
    hostname: localhost
